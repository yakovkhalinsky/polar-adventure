# Copy this file to .env and fill in the real keys.
# .env is gitignored so keys are never committed.

# Black Forest Labs FLUX API key (used with --bfl)
BFL_API_KEY=your_bfl_key_here

# Tripo 3D API key (used with --preview-bear-3d)
TRIPO_API_KEY=your_tripo_key_here
